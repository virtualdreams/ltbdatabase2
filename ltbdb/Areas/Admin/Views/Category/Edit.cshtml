@model ltbdb.Models.CategoryEditContainer
@using ltbdb.Models
@{
	if (Model.Category.Id == 0) {
		ViewBag.Title = "Neue Kategorie | Lustiges Taschenbuch - Administration";
		ViewBag.Headline = "Neue Kategorie";
	} else {
		ViewBag.Title = String.Format("{0} | Lustiges Taschenbuch - Administration", Model.Category.Name);
		ViewBag.Headline = "Kategorie bearbeiten";
	}
}

<div class="box">
	<h3>@ViewBag.Headline</h3>
	<div>
		<form action="@Url.Action("edit", "category", new { area = "admin" })" method="post" enctype="multipart/form-data" id="category-form">
			<div class="element">
				<label for="name">Name</label>
				<input type="text" id="name" name="name" value="@Model.Category.Name" placeholder="Titel der Kategorie" /> @Html.ValidationMessage("name")
			</div>

			<div class="element">
				<input class="button-green" type="submit" value="Speichern" />
			</div>
		</form>
	</div>
</div>
