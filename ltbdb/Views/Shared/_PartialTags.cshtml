@model ltbdb.Models.TagViewContainer
@using ltbdb.Models;

@{
   Layout = null;
}

@foreach (var tag in Model.Tags)
{
	<li><a href="@Url.Action("view", "tag", new { id = tag })">@tag</a></li>
}